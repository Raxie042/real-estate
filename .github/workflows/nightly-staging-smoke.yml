name: Nightly Staging Smoke

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      frontend_url:
        description: 'Optional frontend base URL override (e.g. https://staging.example.com)'
        required: false
        type: string
      api_url:
        description: 'Optional API base URL override (e.g. https://api-staging.example.com/api)'
        required: false
        type: string

jobs:
  smoke-staging:
    runs-on: ubuntu-latest
    env:
      SMOKE_FRONTEND_URL: ${{ inputs.frontend_url != '' && inputs.frontend_url || secrets.STAGING_FRONTEND_URL }}
      SMOKE_API_URL: ${{ inputs.api_url != '' && inputs.api_url || secrets.STAGING_API_URL }}
      SMOKE_CLEANUP: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Initialize smoke artifacts
        run: |
          touch smoke-output.log
          touch staging-smoke-summary.md

      - name: Validate staging URLs
        id: validate
        continue-on-error: true
        run: |
          if [ -z "$SMOKE_FRONTEND_URL" ] || [ -z "$SMOKE_API_URL" ]; then
            echo "Missing staging URL configuration." | tee -a smoke-output.log
            echo "Set STAGING_FRONTEND_URL and STAGING_API_URL repo secrets, or pass workflow_dispatch inputs." | tee -a smoke-output.log
            echo "exit_code=1" >> $GITHUB_OUTPUT
            exit 1
          fi
          echo "Frontend: $SMOKE_FRONTEND_URL" | tee -a smoke-output.log
          echo "API: $SMOKE_API_URL" | tee -a smoke-output.log
          echo "exit_code=0" >> $GITHUB_OUTPUT

      - name: Run smoke E2E against staging
        id: smoke
        if: steps.validate.outputs.exit_code == '0'
        continue-on-error: true
        run: |
          set -o pipefail
          node smoke-e2e.js 2>&1 | tee smoke-output.log
          echo "exit_code=$?" >> $GITHUB_OUTPUT

      - name: Create run summary
        if: always()
        run: |
          RESULT="PASS ✅"
          if [ "${{ steps.validate.outputs.exit_code }}" != "0" ]; then
            RESULT="CONFIG FAIL ❌"
          elif [ "${{ steps.smoke.outputs.exit_code }}" != "0" ]; then
            RESULT="FAIL ❌"
          fi

          {
            echo "# Nightly Staging Smoke Summary"
            echo ""
            echo "- Frontend URL: $SMOKE_FRONTEND_URL"
            echo "- API URL: $SMOKE_API_URL"
            echo "- Result: $RESULT"
            echo "- Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            echo ""
            echo "## Smoke Output"
            echo '```'
            if [ -f smoke-output.log ]; then
              cat smoke-output.log
            else
              echo "No smoke output was captured."
            fi
            echo '```'
          } > staging-smoke-summary.md

      - name: Upload smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: staging-smoke-${{ github.run_id }}
          path: |
            smoke-output.log
            staging-smoke-summary.md

      - name: Publish GitHub step summary
        if: always()
        run: cat staging-smoke-summary.md >> $GITHUB_STEP_SUMMARY

      - name: Fail workflow if validation or smoke failed
        if: steps.validate.outputs.exit_code != '0' || steps.smoke.outputs.exit_code != '0'
        run: exit 1
